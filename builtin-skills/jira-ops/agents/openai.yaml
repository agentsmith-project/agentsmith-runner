interface:
  display_name: "Jira Ops"
  short_description: "Operate Jira through the jira-auth request projection with proxy disabled."
  default_prompt: "Use this skill when the user wants to search issues, inspect issues, add comments, transition issues, or perform other common Jira operations through the opaque jira-auth request projection, especially when requests to the Jira site must clear proxy environment variables first."
